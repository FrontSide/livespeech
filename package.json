{
  "name": "livespeech",
  "version": "1.0.0",
  "description": "Live speech presentation web application",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "dev:host": "cd frontend && npm run dev:host",
    "dev:ip": "cd frontend && HOST_IP=${HOST_IP:-0.0.0.0} HOST=${HOST_IP:-0.0.0.0} npm run dev:host",
    "install:all": "npm install && cd frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
